.git
**/bin
**/obj
**/node_modules
**/.next
dashboard
reference
tests
docs
# contracts/ is NOT excluded: DarkFactory.Contracts embeds
# contracts/schemas/describe.schema.json as a linked resource, so the image
# cannot be built without it (src/Dockerfile, DescribeSchema.cs).
*.md
